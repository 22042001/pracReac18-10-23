import "./Header.css" // importamos el archivo del estilo del header

function Header(){ //creamos una funcion Header con retorno y usamos la etiquetas html para colocar la imagen
    return <header className="header"> 
            <img src = "/img/HeaderHeader_total.png" alt="ghost"/>
    </header>
}

export default Header //esportamos 